/**
 * AI SDK Components
 *
 * This module provides enhanced UI components inspired by AI SDK Elements.
 * These components offer a polished interface for audio recording, playback,
 * voice selection, and transcription features.
 *
 * When @ai-sdk/react is fully integrated, these wrappers can be replaced
 * with native AI SDK components while maintaining the same API.
 */

export { AudioPlayer } from "./AudioPlayer";
export { MicSelector } from "./MicSelector";
export { VoiceSelector, type Voice } from "./VoiceSelector";
